import 'package:flutter/material.dart';

class HSpace extends SizedBox {
  const HSpace(double size, {super.key}) : super(width: size);
}

class VSpace extends SizedBox {
  const VSpace(double size, {super.key}) : super(height: size);
}
